# The core engine (water_arena.market, .agents, .world) needs nothing but the
# Python standard library. These are all optional:
numpy>=1.19        # required for the RL env (water_arena.env)
gymnasium>=0.28    # makes the RL envs true gym.Env (optional)
stable-baselines3>=2.0  # only for examples/rl_train_sb3.py (optional; pulls torch)
matplotlib>=3.3    # draws the charts (optional)
pytest>=7.0        # runs the test suite (optional)
